#pragma once

inline OBJECT numericAdd(OBJECT a, OBJECT b)
{
	// if (GET_ATTRIBUTE(a, type) == "int" && GET_ATTRIBUTE(b, type) == "int")
	// 	return CREATE_INT(GET_VALUE(a, int64_t) + GET_VALUE(b, int64_t));
	// // if (GET_ATTRIBUTE(a, type) == "int" && GET_ATTRIBUTE(b, type) == "float")
	// // 	return CREATE_FLOAT(GET_VALUE(a, int64_t) + GET_VALUE(b, double));
}
